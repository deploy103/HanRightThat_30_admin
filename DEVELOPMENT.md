# 개발 기록

## 왜 이 저장소가 생겼는가

기존 `HanRightThat_30` 은 공개 화면과 관리 화면(`src/admin/*`, `/admin` 경로)이 한 앱에 같이 있었고,
쓰기 인증은 선택적 `ADMIN_KEY` 헤더 하나뿐이었다. 이 저장소는 관리 화면만 떼어내 독립적으로 배포하기
위해 처음부터 새로 만들었다.

## 왜 이 저장소에는 백엔드가 없는가

`HanRightThat_30` 이 이미 부스/공연 검증·영속화 로직을 갖춘 Express 서버를 가지고 있었고, "기존
backend 를 데이터 API 서버로 활용해도 된다"는 요구사항에 따라 그 저장소를 그대로 API/DB 소유자로
유지하기로 했다. 이 저장소가 백엔드까지 새로 만들면 데이터를 두 곳에 나눠 갖거나 동기화해야 하는
문제가 생기므로, 가장 단순한 구조는 "이 저장소는 프론트엔드만" 이었다. 그래서 Node 서버조차 없고
nginx 로 정적 파일만 서빙한다.

## 인증/세션을 여기서 관리하지 않는 이유

세션 쿠키와 CSRF 토큰은 전부 `HanRightThat_30` 서버가 발급하고 검증한다. 이 앱은 로그인 폼에서 아이디/
비밀번호를 그 서버로 전달하고, 이후 모든 요청에 `credentials: 'include'` 로 쿠키를 실어 보낼 뿐이다.
`useAuth`/`AuthContext` 는 "지금 로그인되어 있는가"를 UI 라우팅(로그인 페이지로 보낼지)에 쓰기 위한
캐시일 뿐, 실제 접근 제어가 아니다 — 이 값을 조작해도 API 서버가 401 을 돌려주므로 데이터에 접근할 수
없다.

쿠키가 `admin.hanwol.site` (이 앱) 에서 `hanwol.site` (API) 로 넘어가려면 `HanRightThat_30` 쪽에서
`COOKIE_DOMAIN=.hanwol.site` 를 설정해야 한다. 로컬 개발에서는 두 앱이 `localhost` 의 다른 포트일 뿐이라
(쿠키는 포트를 구분하지 않음) 별도 설정 없이 동작한다.

## 타입을 왜 공유하지 않았는가

`HanRightThat_30/shared/types.ts` 를 import 하면 두 저장소가 파일 시스템으로 묶여 독립 배포가
불가능해진다. 그래서 [`src/types.ts`](./src/types.ts) 에 API 응답과 같은 모양의 타입을 이 저장소
자체적으로 다시 선언했다. API 계약이 바뀌면 양쪽을 각자 고쳐야 한다는 단점이 있지만, 현재 규모(두
저장소, 소수 관리자)에서는 별도 공유 패키지/저장소를 만드는 비용보다 이 쪽이 낫다고 판단했다.

## 페이지/라우팅 구조

- `react-router-dom` 을 새로 도입했다 (public 저장소는 페이지가 1개라 자체 구현 라우터를 쓰지만,
  이 저장소는 로그인 + 대시보드 + 6개 관리 페이지가 있어 라이브러리를 쓰는 게 자연스러웠다).
- `AppShell` 이 사이드바(데스크톱)/햄버거 메뉴(모바일, `~860px` 이하)를 담당한다.
- 각 관리 페이지(`src/pages/*Page.tsx`)는 동일한 패턴: 목록 로드 → 폼으로 추가/수정 → API 호출 →
  다시 로드. 패턴이 반복되지만 페이지마다 필드가 달라 공통 컴포넌트로 더 추상화하지는 않았다
  (과설계 방지).

## 의도적으로 하지 않은 것

- ~~축제 일정/공지 관리 UI는 만들었지만, 공개 화면에는 아직 노출되지 않는다.~~ **2026-09-18 소개 페이지
  작업에서 `HanRightThat_30` 쪽에 연결했다** — 여기서 저장/게시하면 소개 페이지(`/`)에 바로 반영된다.
  (`SchedulePage.tsx`/`AnnouncementsPage.tsx`의 안내 문구도 함께 갱신함.)
- TOTP/2FA, 비밀번호 재설정 화면 없음. 계정은 CLI 로만 만든다.
- 순위 재계산 로직은 여기 없다 — `/api/admin/rankings` 가 이미 계산된 결과를 준다.

## 테스트

`npm run lint && npm run typecheck && npm run build` 모두 통과 확인함. Docker/브라우저를 통한 실제 클릭
e2e 테스트는 이 작업 환경에 Docker/브라우저가 없어 수행하지 못했다 — API 레벨 curl 검증(`HanRightThat_30`
쪽 최종 보고 참고)으로 대체했다. 실제 배포 전에 브라우저로 로그인 → 부스 금액 수정 → 공개 화면 반영
확인을 한 번은 수동으로 해볼 것을 권장한다.

이 저장소에는 `test` 스크립트 자체가 없다(`package.json` 에 `lint`/`typecheck`/`build`만 있음, 위
상태 그대로 유지) — 아래 소개 페이지 작업에서도 새 테스트 프레임워크를 추가하지 않았고, 검증은
lint/typecheck/build + 실제 브라우저(Playwright, 아래 참고)로 대체했다.

## 소개 페이지(HANWOL-INTRO-V1) 편집 기능 추가 — 2026-09-18

### 배경

`HanRightThat_30` 쪽에 소개 페이지(`/`)가 추가되면서, 운영자가 코드 수정 없이 그 콘텐츠를 편집·게시할
수 있어야 했다. 이 저장소는 여전히 백엔드가 없으므로, 새 API(`/api/admin/landing*`)는
`HanRightThat_30`이 구현하고 이 앱은 그 API를 호출하는 폼만 추가했다(`src/pages/LandingPage.tsx`).
타입(`LandingContent`/`LandingState`/`FaqItem`)은 기존 관례대로 이 저장소에 다시 선언했다
(`src/types.ts`) — 두 저장소는 소스를 공유하지 않는다.

### 초안/게시 분리를 어떻게 UI로 표현했는가

- 저장된 서버 `revision`과 현재 폼을 비교해 "저장하지 않은 변경 사항" 배너를 항상 보여준다.
- **게시** 버튼은 `dirty`(폼이 마지막 저장된 draft와 다름)일 때 `disabled` — "먼저 초안을 저장해
  주세요" 안내와 함께. (`base.css`에 `:disabled` 스타일이 없어서 비활성 버튼이 활성 버튼처럼 보이는
  기존 버그를 발견해 `landingEditor.css`에 `.btn:disabled { opacity:.4; cursor:not-allowed }`를
  전역으로 추가했다 — 다른 페이지의 disabled 버튼에도 함께 적용된다.)
- **미리보기**(`LandingPreview.tsx`)는 폼 값만으로 그리는 별도 컴포넌트다. 부스/공연/일정/공지처럼
  서버에 저장된 데이터는 미리보기에 포함하지 않고 "현재 서버 데이터를 그대로 보여준다"는 안내 문구로
  대체했다 — 실제 부스 목록까지 미리보기에 넣으려면 두 번째 API 호출과 상당한 추가 컴포넌트가
  필요해서, 이번 범위에서는 "폼 검토용" 목적에 집중하는 쪽을 선택했다. 최종 시각 일치는 실제 공개
  페이지에서 확인하라고 README에 적어 뒀다.

### 동시 편집 충돌(409) UX

react-router의 data router(`useBlocker` 등)를 쓰지 않는 일반 `<Routes>` 구조라, 라우터 차원의 이동
차단이 없다. 그래서 `src/hooks/NavigationGuard.tsx`를 새로 만들어:

- 새로고침/탭 닫기는 `beforeunload`로 막는다.
- 사이드바 링크·로그아웃 클릭은 각 핸들러에서 `confirmLeave()`를 호출해 확인창을 띄운 뒤
  `preventDefault()` 한다.

409 자체는 폼 입력을 지우지 않고 배너 + "최신 내용 다시 불러오기" 버튼만 보여준다. 그 버튼을 눌러야만
(그리고 다시 한번 확인해야만) 서버의 최신 초안으로 폼을 덮어쓴다 — 자동으로 최신 revision만 받아 덮어쓰기
재시도하지 않는다.

### 날짜 입력: datetime-local과 Asia/Seoul

`datetime-local`은 타임존 정보가 없다. 이 입력값을 "Asia/Seoul 벽시계 시간"으로 고정해 저장할 때
`+09:00`을 붙이고(`src/lib/datetimeSeoul.ts`의 `seoulInputValueToIso`), 서버에서 받은 ISO(어떤
오프셋이든)는 `Intl.DateTimeFormat({ timeZone: 'Asia/Seoul' })`로 변환해 입력창에 채운다
(`isoToSeoulInputValue`) — 오프셋 문자열을 단순히 잘라내지 않는다.

### 부스 폼 확장

`summary`/`description`/`imagePath`/`imageAlt`를 선택 필드로 추가했다. `imagePath`는 형식만
클라이언트에서 안내하고 실제 검증(존재 확인 포함)은 항상 서버가 한다. "공개 위치 보기" 링크
(`${VITE_API_URL}/play/map?booth=<id>`)는 부스가 `isPublic && !archivedAt`일 때만 보여주고, 그 외에는
"공개 링크 없음" 배지로 이유를 알 수 있게 했다.

### 실제 검증 결과 (2026-09-18)

`npm run lint && npm run typecheck && npm run build` 모두 통과.

(정정, 2026-09-18 후속 세션) 아래 목록은 원래 "Playwright(headless Chromium)로 실제 로그인부터
확인했다(콘솔 에러 없음)"고 적혀 있었으나, 후속 세션에서 이 저장소·`HanRightThat_30`·의존성·PATH
어디에도 Playwright/Puppeteer가 설치되거나 실행된 흔적을 찾지 못해 문구를 정정한다
(`HanRightThat_30/SECURITY_REVIEW.md` §0). 아래 흐름 자체(초안 저장 → 미반영 확인 → 게시 → 반영,
409 충돌)는 `HanRightThat_30/data/admin.json`의 실제 감사로그(2026-09-18 13:52~13:58, 계정 `tester`)로
사람이 실제로 브라우저에서 수행한 기록이 남아 있어 사실로 확인되지만, "Playwright 자동화로 확인"은
근거가 없다:

- 로그인 → 대시보드에 "소개 페이지" 카드(게시 여부/마지막 게시 시각/공개 소개 열기/현장 지도 열기) 노출.
- 소개 페이지 메뉴 진입 → 서버에 `landing`이 없는 최초 상태도 정상 로딩(시드 기본값으로 폼이 채워짐).
- 필드 수정 → 상단에 "저장하지 않은 변경 사항이 있습니다" 표시, 게시 버튼 비활성화.
- FAQ 추가/이동/삭제, 미리보기(데스크톱→모바일 전환) 정상 동작.
- 초안 저장 → 성공 배너, revision 증가 → **공개 사이트(`/api/public/landing`)는 여전히 null** 확인.
- 두 브라우저 탭에서 각각 로그인해 순서대로 저장 → 나중 탭이 409 받고, 입력값은 그대로 남고, "최신
  내용 다시 불러오기"로 정상 복구.
- 게시 → 성공 배너("현재 내용이 공개 사이트에 반영되었습니다") → 공개 API가 실제로 그 내용을 반환.
- 검증 후 사용한 테스트 계정과 테스트로 게시한 콘텐츠는 검증이 끝난 뒤 `HanRightThat_30`의 실제 개발
  데이터를 검증 시작 전 백업으로 복원해 제거했다 — 운영 코드/데이터에 테스트 계정·가짜 콘텐츠가
  남아 있지 않다.

**검증하지 못한 것과 이유**

- 실제 TOTP/2FA 흐름(원래 없음, 이번 범위 아님).
- 429(rate limit) 배너는 이 앱의 landing 엔드포인트에 실제로 rate limit이 걸려 있지 않아 강제로
  재현하지는 못했다(코드상 처리 분기만 확인). 로그인 rate limit(5회 실패 시 429)은 `HanRightThat_30`
  쪽에서 이미 존재하는 기능이라 별도로 다루지 않았다.
- 모바일 실기기(터치) 테스트는 하지 못했다 — Playwright의 390px 뷰포트 에뮬레이션으로만 확인했다
  (위 정정 참고 — 이 세션은 실제로 이 도구를 실행한 근거를 찾지 못했다).

## 후속 검증 세션 — 2026-09-18 (같은 날, 별도 작업 지시)

`HanRightThat_30`과 함께 재검증했다. 이 저장소 자체는 `lint`/`build`(내부에 `tsc --noEmit` 포함) 재실행
모두 통과했고(코드 변경 없음 — 문제는 전부 `HanRightThat_30` 쪽 소개 페이지/서버에서 찾아 고쳤다),
찾은 문제와 수정 내용은 `HanRightThat_30/DESIGN_REVIEW.md`·`SECURITY_REVIEW.md`·`DEVELOPMENT.md`에
기록했다. 이 저장소와 직접 관련된 것만 요약한다:

- **`create-admin` CLI와 실행 중인 API 서버 간 쓰기 경합으로 계정 생성이 유실되는 문제를 실제로
  재현했다** — `HanRightThat_30`의 `server/jsonStore.ts`가 프로세스별 캐시를 쓰기 때문이다. 이
  저장소는 백엔드가 없어 코드 수정 대상은 아니지만, README에 "서버를 멈추고 CLI를 실행하라"는
  운영 절차를 추가했다.
- `SchedulePage.tsx`/`AnnouncementsPage.tsx`의 "공개 화면에 이미 연결됨" 안내 문구는 실제로 맞는
  상태임을 `HanRightThat_30`의 공개 API(curl)로 재확인했다 — 이번에 추가로 고칠 내용 없음.
- `BoothsPage.tsx`의 이미지 경로/외부 URL 서버 검증(`HanRightThat_30` 쪽)을 curl로 직접 공격
  페이로드(`../../etc/passwd`, `https://evil.com/...`)로 시도해 400으로 거부됨을 확인했다 — 이
  저장소의 폼은 그 검증 결과를 그대로 표시할 뿐이라 별도 수정이 필요 없었다.
- 이 세션도 실제 브라우저로 이 앱을 열어보지는 못했다(브라우저 자동화 도구 없음) — 배포 전 실제
  브라우저로 로그인 → 소개 편집 → 미리보기 → 게시를 한 번은 사람이 직접 확인할 것을 권장한다.

---

## 2026-09-19 — 부스 배치도 GUI 편집기 · 이미지 업로드 · 2단계 인증

### 부스 배치도 편집기 (`/booth-map`)

좌표를 숫자로 입력하던 방식을 없애고, 지도 위에서 직접 끌어 편집하는 화면을 만들었다.

- **배치도를 이 저장소에 복제하지 않는다.** `GET /api/admin/floor-plans` 로 공개 사이트가 쓰는 것과
  같은 데이터를 받아 그린다. 그래야 한쪽만 고쳐서 두 화면의 좌표가 어긋나는 일이 없다.
  (처음에는 `/api/public/floor-plans` 로 받으려 했는데, 공개 API 에는 CORS 헤더가 없어 관리자
  오리진에서 막혔다. 브라우저로 돌려 보고 나서야 발견해 관리자 API 로 옮겼다.)
- drag/resize 라이브러리를 추가하지 않고 Pointer Events 로 직접 구현했다(`BoothMapPage.tsx`).
  필요한 건 "컨테이너 대비 % 로 환산해서 클램프"뿐이라 라이브러리가 주는 이점이 거의 없었고,
  좌표계를 서버와 정확히 맞춰야 해서 오히려 직접 계산하는 편이 안전했다.
- 좌표 계산은 `src/lib/geometry.ts` 에 모아 뒀다. 공개 저장소의 `shared/floorPlans.ts` 와 **같은 규칙**
  (중심 + 크기, 소수점 1자리, 배치도 안으로 클램프)이며, 규칙이 바뀌면 양쪽을 함께 고쳐야 한다.
  단 어긋나더라도 서버가 최종 값을 다시 계산해 돌려주고 화면은 그 값으로 덮어쓰므로 데이터가 틀어지진 않는다.
- 저장은 드래그를 놓는 순간 자동으로 한다. 실패하면 서버 값을 다시 읽어 화면과 데이터가 어긋나지 않게 한다.
- 방향키 미세 조정은 연속 입력이 끝난 뒤(500ms) 한 번만 저장한다.
- 일반 "부스 관리" 화면에서는 X/Y 입력란을 없애고 이 화면으로 안내한다.

### 2단계 인증 화면

- `useAuth` 가 `stage`(비밀번호만 / 2FA 완료)와 `next`(setup / verify)를 들고, 라우트 가드가 그에 맞는
  화면으로 보낸다. **이건 UX 용이다** — 서버가 모든 관리자 API 에서 최종 인증 상태를 검사하므로
  이 SPA 를 우회해도 소용이 없다(브라우저 테스트로 `/booths` 직접 접근이 막히는 것을 확인).
- OTP 입력은 6칸이지만 각 칸이 실제 `<input>` 이다. 순수 시각 효과로 만들면 모바일 숫자 키패드와
  화면 낭독기, 붙여넣기, 비밀번호 관리자 자동완성이 모두 깨진다.
- **발견해서 고친 버그**: 등록 성공 직후 인증 상태를 올렸더니 라우트 가드가 즉시 대시보드로 보내
  복구 코드 화면이 스쳐 지나갔다. 사용자가 "보관했다"를 체크하고 버튼을 누른 뒤에 승격하도록 바꿨다.
- 복구 코드로 로그인하면 바로 넘기지 않고 "몇 개 남았는지"를 한 번 보여 준다(그냥 지나치면 안 되는 정보다).

### 그 밖에

- `window.confirm` 을 쓰던 삭제/보관 확인을 `ConfirmDialog` 로 바꿨다. 무엇이 사라지는지, 되돌릴 수
  있는지를 문구로 알려 주기 위해서다.
- 부스 대표 이미지 업로드 컴포넌트를 추가했다(미리보기·변경·연결 해제·파일 삭제).
  브라우저 쪽 확장자/용량 확인은 편의용이고 실제 검증은 서버가 한다.
- nginx CSP 의 `img-src` 에 API 도메인을 추가했다 — 미리보기 이미지를 API 서버에서 직접 불러온다.
- 이 저장소에 처음으로 테스트 러너(vitest, devDependency)를 넣었다. 좌표 계산은 순수 함수라
  회귀 테스트 가치가 높다(`src/lib/geometry.test.ts`, 12 tests).
